import { HighvotesDirective } from './highvotes.directive';

describe('HighvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new HighvotesDirective();
    expect(directive).toBeTruthy();
  });
});
